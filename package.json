{
  "name": "tailwind",
  "version": "1.0.0",
  "description": "tailwind",
  "main": "index.js",
  "scripts": {
    "start": "vite",
    "build:css": "tailwindcss build src/styles.css -o dist/styles.css",
  "build": "npm run build:css && cp -r src/*.html dist/"
  },
  
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4"
  },
  "dependencies": {
    "feather": "^0.0.6",
    "vite": "^5.3.2"
  }
}
